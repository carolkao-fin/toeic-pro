# TOEIC Pro 850+ — Streamlit 版開發文件

## 專案概述

| 項目 | 內容 |
|------|------|
| 專案名稱 | TOEIC Pro 850+ 多益學習平台 |
| 開發工具 | Claude Code (claude-sonnet-4-6) |
| 開發日期 | 2026-05-07 起 |
| 技術架構 | Python + Streamlit，部署於 Streamlit Cloud |
| GitHub | https://github.com/carolkao-fin/toeic-pro |
| 目標 | 幫助使用者達到多益 850 分以上 |

---

## 開發歷程

### 第一版（HTML 原型）
純前端 HTML + CSS + JavaScript 單檔案，120 個自建單字、30 題文法、4 篇閱讀，localStorage 儲存進度。
部署於 Netlify：https://tourmaline-llama-356f4a.netlify.app/

### 第二版（Streamlit 版，850+ 目標）
**使用者需求：**
> 升級至 Streamlit、目標 850 分、新增每日打卡 + 小遊戲、使用開放授權詞彙資源

**重大改變：**
- 技術架構從 HTML 改為 Python + Streamlit
- 單字庫從 120 筆自建 → **2,846 筆開源資料集**（CC-BY-SA-4.0）
- 文法題庫從 30 題 → **54 題**
- 新增每日打卡系統（連續打卡、勳章、日曆）
- 新增三種學習小遊戲
- 分數目標從 750+ 提升為 **850+**

### 第三版（Bug 修正 + 功能優化，2026-05-08）

**修正清單：**

| # | 問題 | 原因 | 修正方式 |
|---|------|------|----------|
| 1 | 拼字遊戲執行時崩潰 | `_init_speed_game()` 直接存 VOCAB dict，但 `_run_speed_game()` 讀取 `w["en"]`（VOCAB 使用 `"word"` key） | 初始化時統一轉換為 `{"en": word, "zh": zh}` |
| 2 | 單字過濾邏輯錯誤 | `word[:2] in ("an","th")` 會誤刪 analyze、annual、through 等字 | 改為 `" " in word`（有空格才是片語） |
| 3 | 快速測驗答錯不標紅 | 未記錄使用者選擇的選項（只有 `gq_was_correct`） | 新增 `gq_chosen` 追蹤選項，答錯顯示紅色 |
| 4 | AI 出題顯示舊數字 | 文字寫死「從 120 個單字庫中」 | 改為動態 `f"{len(VOCAB):,} 個"` |
| 5 | 單字庫缺分數區間欄位 | 未顯示 range 欄位 | 加上彩色標籤（藍=780-900、紫=900+、綠=600-780） |
| 6 | 單字卡缺分數區間篩選 | 只有分類篩選 | 新增 600-780 / 780-900 / 900+ 下拉篩選 |

**新增：**
- 單字庫加入資料來源注記（CC-BY-SA-4.0 授權連結）
- 新增 `.gitignore`（排除 `checkin_data.json` 使用者資料）

### 第四版（緊急修正，2026-05-08）

**問題：** Streamlit Cloud 啟動時報錯 `StreamlitDuplicateElementId`

**原因：** `page_vocab()` 與 `page_wordlist()` 都有 label 為「分數區間」的 `st.selectbox()`，Streamlit 依 label 產生元件 ID 導致衝突。

**修正：** 為兩個 selectbox 加上唯一 `key` 參數：
```python
# page_vocab
st.selectbox("分數區間", ranges, key="vocab_range")

# page_wordlist
st.selectbox("分數區間", [...], key="wordlist_range")
```

**注意事項：** 同一 Streamlit app 中，跨分頁若有相同 label 的 widget，必須指定不同 `key` 避免衝突。

### 第五版（單字庫播放功能，2026-05-08）

**使用者需求：** 在單字庫（📋）頁面新增逐筆單字的語音播放按鈕。

---

（以下為 2026-05-08 第二批開發）

---

### 第六版（筆記本功能，2026-05-08）

**使用者需求：** 新增「📒 筆記本」Tab，讓使用者自由輸入筆記內容，不限於單字用途。

**實作內容：**

新增獨立 Tab `📒 筆記本`，位於 `🤖 AI 出題` 後面，包含兩個子分頁：

#### 子分頁一：📝 自由筆記

| 功能 | 說明 |
|------|------|
| 新增筆記 | 輸入標題 + 任意內容，點擊「💾 儲存新筆記」 |
| 搜尋篩選 | 全文搜尋標題與內容 |
| 編輯 | 點擊 ✏️ 展開編輯欄位，儲存後自動更新修改日期 |
| 刪除 | 🗑️ 逐筆刪除，或「清空全部」 |
| 資料持久化 | 儲存至 `notes_data.json` |

**筆記資料格式：**
```python
{
    "id": "1746700000",           # 建立時間戳
    "title": "使用者自訂標題",
    "content": "自由文字內容",
    "date_created": "2026-05-08",
    "date_modified": "2026-05-08",
}
```

#### 子分頁二：📖 自訂單字（第七版，見下方）

**單字卡整合：** 在 `📚 單字卡` 頁面新增第五個按鈕「📌 加入筆記」，點擊後以單字資訊預填標題與內容，自動建立一則筆記：
```
標題：allocate
內容：v. 分配

例句：The manager will allocate resources carefully.

筆記：（使用者可繼續編輯）
```

---

### 第七版（自訂單字庫，2026-05-08）

**使用者需求：** 讓使用者自行加入新的單字，並與現有練習系統彙整。

**實作內容：**

在 `📒 筆記本 → 📖 自訂單字` 子分頁加入完整的自訂單字管理功能：

**新增表單欄位：**

| 欄位 | 類型 | 必填 |
|------|------|------|
| 英文單字 | 文字輸入 | ✅ |
| 中文意思 | 文字輸入 | ✅ |
| 詞性 | 下拉選單（n./v./adj./adv./prep./conj./phrase） | — |
| 分類 | 文字輸入（預設「自訂」） | — |
| 例句 | 文字區域 | — |

**自訂單字資料格式：**
```python
{
    "word": "ephemeral",
    "pos": "adj.",
    "zh": "短暫的",
    "ex": "Social media fame is often ephemeral.",
    "cat": "自訂",
    "range": "自訂",
    "date_added": "2026-05-08",
}
```

**彙整機制（`get_combined_vocab()`）：**

```python
def get_combined_vocab() -> list:
    custom = load_custom_vocab()
    combined = list(VOCAB)
    existing = {w["word"].lower() for w in VOCAB}
    for c in custom:
        if c["word"].lower() not in existing:
            combined.append(c)
    return combined
```

自訂單字自動合併進所有使用 `VOCAB` 的功能頁面：

| 頁面 | 效果 |
|------|------|
| 📚 單字卡 | 分類篩選出現「自訂」，可練習自訂單字 |
| 📋 單字庫 | 搜尋、篩選結果包含自訂單字（粉紅色邊框區分） |
| ⏱ 模擬考 | 單字題來源改用合併詞庫 |

**分類篩選設計：**

「自訂」選項永遠固定出現在兩個頁面的「分類」下拉選單中，即使尚未新增任何自訂單字，方便使用者知道此功能存在：

```python
_all_cats = sorted(set(w["cat"] for w in get_combined_vocab()))
if "自訂" not in _all_cats:
    _all_cats.append("自訂")
cats = ["全部"] + _all_cats
```

---

### 第八版（空結果防護，2026-05-08）

**問題：**
1. `📚 單字卡` 選「自訂」分類但尚未新增任何自訂單字時，`total_w = 0` 導致 `ZeroDivisionError`（Streamlit Cloud 出現紅色錯誤頁面）
2. `📋 單字庫` 篩選結果為空時先印出「顯示 0/2529 筆」，再才顯示提示訊息

**修正：**

```python
# 單字卡：篩選後立刻檢查
words = vocab_filtered()
if not words:
    st.info("此分類目前沒有單字。請前往「📒 筆記本 → 自訂單字」新增，或更換分類篩選。")
    return  # 提早結束，不執行除法

# 單字庫：先判斷空再顯示筆數
if not filtered:
    st.info("此分類目前沒有單字。...")
    return
st.markdown(f"顯示 {len(filtered)}/{len(_all_vocab)} 筆")  # 有結果才顯示
```

**設計原則：** 所有涉及篩選的頁面，都在進入渲染邏輯前先做空結果判斷，避免因使用者操作觸發除以零或空列表錯誤。

**實作方式：**

將 `page_wordlist()` 原本逐行呼叫 `st.markdown()` 的清單改為單一 `components.html()` 區塊，並在每列最右側嵌入 🔊 按鈕：

```python
# 每列加入播放按鈕（inline onclick → Web Speech API）
f'<button class="speak-btn" onclick="speakWord(\'{word_js}. {ex_js}\')" title="播放發音">🔊</button>'

# 容器高度依篩選筆數動態調整
h = min(600, max(200, len(filtered) * 48))
components.html(f"""...<div style="height:{h}px;overflow-y:auto">...</div>
<script>
function speakWord(t){{
  window.speechSynthesis.cancel();
  var u=new SpeechSynthesisUtterance(t);
  u.lang='en-US';u.rate=0.88;u.pitch=1;
  ...
  window.speechSynthesis.speak(u);
}}
</script>""", height=h+4, scrolling=False)
```

**設計決策：**

| 問題 | 解決方式 |
|------|----------|
| `st.markdown()` 內無法執行 JS | 改用 `components.html()` 渲染整個清單 |
| 跨分頁 CSS 隔離（iframe） | 在 `components.html()` 內重新定義所有需要的 CSS |
| 大量單字時效能 | 由一次 `components.html()` 代替 N 次 `st.markdown()` |
| 捲動容器高度 | `min(600, max(200, 筆數 × 48))` 動態計算，避免空白過多 |
| JS 字串安全（單引號） | `word_js = word.replace("'", "\\'")` 跳脫 onclick 引號 |

**播放內容：** 單字 + 例句（與單字卡頁一致）
**語音設定：** `en-US`、rate 0.88、偏好英語聲音（與聽力頁 `speak_js()` 相同參數）

---

## 功能清單

| 分頁 | 功能 | 說明 |
|------|------|------|
| 🏠 首頁 | 學習儀表板 | 累計作答、正確率、預估分數、科目進度條 |
| 📅 打卡 | 每日打卡 | 連續天數、當月日曆、10 個成就勳章、XP 積分 |
| 🎮 遊戲 | 三種小遊戲 | 配對、快問快答、極速拼字（各有計時器與 XP） |
| 📚 單字卡 | 翻牌練習 | 2,846 筆單字，按分數區間篩選，標記已學/未學 |
| 📝 文法 | Part 5 測驗 | 54 題，涵蓋時態/詞性/介系詞/固定搭配/連接詞 |
| 🎧 聽力 | Part 2/3/4 | Web Speech TTS 播放，應答/對話/獨白三種題型 |
| 📖 閱讀 | Part 7 測驗 | 4 篇文章，Email/Notice/Advertisement/Memo |
| ⏱ 模擬考 | 50 題 / 30 分 | 文法20+閱讀20+單字10，倒數計時+顏色警示 |
| 🔴 錯題 | 錯題複習 | 自動收集所有答錯題目，針對性再練習 |
| 📋 單字庫 | 完整單字表 | 依詞性/分類/分數區間篩選，支援搜尋，🔊 逐筆播放發音 |
| 🤖 AI 出題 | 隨機生成選擇題 | 從單字庫隨機抽詞，模板引擎生成 MCQ |
| 📒 筆記本 | 自由筆記 + 自訂單字 | 可輸入任意筆記、新增自訂單字並整合至單字卡與單字庫練習 |

---

## 系統架構

```
toeic-pro/
├── app.py              # 主程式（全部功能）
├── vocab_data.json     # 2,846 筆單字（CC-BY-SA-4.0）
├── fetch_vocab.py      # 從 Hugging Face 下載/更新單字
├── checkin_data.json   # 打卡紀錄（自動生成）
├── notes_data.json     # 自由筆記（自動生成）
├── custom_vocab.json   # 使用者自訂單字（自動生成）
├── requirements.txt    # streamlit>=1.31.0
└── 多益開發紀錄.md     # 本文件
```

### app.py 模組結構

```
app.py
├── CSS 樣式（st.markdown + unsafe_allow_html）
├── 資料層
│   ├── _load_vocab()         讀取 vocab_data.json
│   ├── GRAMMAR_QS[54]        文法題庫
│   ├── LISTEN_P2/P3/P4       聽力題庫
│   └── READINGS[4]           閱讀文章
├── 打卡模組
│   ├── load_checkin()        讀取 checkin_data.json
│   ├── save_checkin()        寫入 checkin_data.json
│   ├── calc_streak()         計算連續天數
│   └── check_badges()        解鎖勳章判斷
├── 遊戲模組
│   ├── _init_match_game()    配對遊戲初始化
│   ├── _run_match_game()     配對遊戲邏輯
│   ├── _init_quiz_game()     快問快答初始化
│   ├── _run_quiz_game()      快問快答邏輯
│   ├── _init_speed_game()    極速拼字初始化
│   ├── _run_speed_game()     極速拼字邏輯
│   └── _end_game()           結算遊戲
├── 筆記模組
│   ├── load_notes() / save_notes()       自由筆記 JSON 讀寫
│   ├── load_custom_vocab()               自訂單字 JSON 讀寫
│   ├── save_custom_vocab()
│   ├── get_combined_vocab()              合併 VOCAB + 自訂單字
│   ├── word_in_notes()                   判斷單字是否已在筆記
│   └── add_to_notes()                    從單字卡快速建立筆記
├── 學習模組（各 page_xxx() 函數）
│   ├── page_home()
│   ├── page_checkin()
│   ├── page_games()
│   ├── page_vocab()
│   ├── page_grammar()
│   ├── page_listening()
│   ├── page_reading()
│   ├── page_mock()
│   ├── page_review()
│   ├── page_wordlist()
│   ├── page_ai()
│   ├── page_notebook()           筆記本路由（含兩個子分頁）
│   ├── _section_free_notes()     自由筆記子分頁
│   └── _section_custom_vocab()   自訂單字子分頁
└── main()                    st.tabs() 路由（12 個分頁）
```

---

## 資料設計

### 單字資料來源

| 項目 | 內容 |
|------|------|
| 資料集 | `kknono668/toeic-vocab-tw`（Hugging Face） |
| 授權 | CC-BY-SA-4.0（商業可用、署名、相同方式分享） |
| 筆數 | 2,846 筆（篩選後） |
| 分數區間 | 600-780 / 780-900 / 900+（優先顯示 780-900） |
| 語言 | 英文單字 + 繁體中文釋義 + 英文例句 |

**單字欄位結構：**
```python
{
    "word": "abate",
    "pos": "v.",           # 詞性（v./n./adj./adv./prep.）
    "cat": "營運管理",     # 分類
    "zh": "（風、雨等）減弱，緩和",
    "ex": "The storm is expected to abate by tomorrow morning.",
    "range": "780-900"     # TOEIC 分數區間
}
```

**更新單字資料：**
```bash
python fetch_vocab.py   # 重新從 Hugging Face 下載，覆蓋 vocab_data.json
```

### 文法題型分布（54 題）

| 考點 | 題數 |
|------|------|
| 被動語態（was/were/be + p.p.） | 10 題 |
| 詞性辨別（名詞/形容詞/副詞） | 12 題 |
| 介系詞搭配（at/in/on/for/by/with） | 8 題 |
| 固定片語（take effect / comply with / thanks to） | 8 題 |
| 動詞時態（完成式、條件句、不定詞） | 8 題 |
| 連接詞與從屬子句（although / once / due to） | 8 題 |

### 聽力題型結構

```
Part 2 應答題（10 題）
  └── TTS 播放問句 → 三選一最佳應答

Part 3 對話題（2 段 × 3 題 = 6 題）
  └── TTS 多行遞迴播放對話 → 理解問題

Part 4 獨白題（2 篇 × 3 題 = 6 題）
  └── TTS 播放廣播/公告 → 理解問題
```

### 閱讀文章（4 篇 × 4 題 = 16 題）

| 文章 | 題型 | 難度 |
|------|------|------|
| Email: Q3 Project Update | 主旨、細節、詞彙 | ★★☆ |
| Notice: Office Renovation | 細節、推論 | ★★☆ |
| Advertisement: Senior Marketing Manager | 職缺資訊、NOT 題 | ★★★ |
| Memo: Remote Work Policy | 政策細節、時間 | ★★★ |

---

## 核心功能實作

### 1. 打卡系統

資料以 JSON 檔案儲存（非 session_state），確保重新整理後不遺失：

```python
CHECKIN_FILE = Path(__file__).parent / "checkin_data.json"

def load_checkin():
    if not CHECKIN_FILE.exists():
        return {"checkins": [], "badges": [], "xp": 0}
    return json.loads(CHECKIN_FILE.read_text(encoding="utf-8"))

def save_checkin(data):
    CHECKIN_FILE.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")

def calc_streak(checkins):
    # 從今天往回數連續天數
    ...
```

**10 個成就勳章：**

| 勳章 | 條件 |
|------|------|
| 🌱 初次打卡 | 累計 1 天 |
| 🔥 熱身週 | 連續 3 天 |
| 💪 習慣養成 | 連續 7 天 |
| 🌟 雙週達人 | 連續 14 天 |
| 👑 月度冠軍 | 連續 30 天 |
| 📚 累積 10 天 | 累計 10 天 |
| 🎯 累積 30 天 | 累計 30 天 |
| 🏆 百日學霸 | 累計 100 天 |
| 🎮 遊戲初體驗 | 完成 1 次遊戲 |
| ⚡ 遊戲達人 | 完成 10 次遊戲 |

### 2. 小遊戲

| 遊戲 | 時限 | XP | 玩法 |
|------|------|----|------|
| 🃏 單字配對 | 90 秒 | 最多 50 XP | 點擊英文卡與中文卡配對 |
| ⚡ 快問快答 | 120 秒 | 最多 100 XP | 10 題文法選擇題 |
| ⌨️ 極速拼字 | 60 秒 | 最多 80 XP | 看中文輸入正確英文單字 |

計時器以 `game_end_time = time.time() + seconds` 儲存在 session_state，每次 rerun 計算剩餘秒數。

### 3. 模擬考（50 題 / 30 分鐘）

```python
def build_mock():
    qs = []
    # 文法 20 題（從 54 題隨機抽）
    for q in random.sample(GRAMMAR_QS, 20):
        qs.append({**q, "source": "grammar"})
    # 閱讀 20 題（從 4 篇文章）
    ...
    # 單字 10 題（AI 隨機生成 MCQ）
    ...
    return random.sample(qs, len(qs))
```

**計時器顏色警示：**
- 正常（> 10 分）：藍色
- 警告（5-10 分）：黃色
- 緊急（< 5 分）：紅色

### 4. 預估多益分數（850 目標）

```python
def est_toeic():
    acc = S.correct / S.total
    if acc >= 0.96: return "990 🏆"
    if acc >= 0.92: return "~950"
    if acc >= 0.87: return "~850 ✨"   # 目標區間
    if acc >= 0.80: return "~800"
    if acc >= 0.72: return "~730"
    if acc >= 0.62: return "~650"
    return "~550"
```

**達到 850 分的正確率目標：** 整體正確率需穩定在 **87% 以上**

### 5. 單字庫播放發音

單字庫頁以 `components.html()` 渲染整個可捲動清單，每列嵌入 🔊 按鈕：

```python
word_js = w["word"].replace("\\", "\\\\").replace("'", "\\'")
ex_js   = w["ex"].replace("\\", "\\\\").replace("'", "\\'")
# onclick 直接呼叫 speakWord()，無需 Streamlit rerun
f'<button class="speak-btn" onclick="speakWord(\'{word_js}. {ex_js}\')">🔊</button>'
```

容器高度 `h = min(600, max(200, len(filtered) * 48))`，超出時在 iframe 內捲動，不影響外部頁面佈局。

### 6. AI 自動出題

從 2,846 筆單字庫隨機抽詞，以模板引擎生成選擇題，干擾選項從相同詞性的單字中選取：

```python
def gen_mcq(word_obj):
    templates = [
        "The company plans to _____ the new strategy.",
        "She was recognized for her _____ performance.",
        ...
    ]
    wrong_opts = [w for w in VOCAB if w["pos"] == word_obj["pos"] and w != word_obj]
    ...
```

---

## 設計系統

### CSS 變數

```css
:root {
  --pr: #2563eb;    /* 主色（藍）*/
  --gr: #10b981;    /* 綠—答對 */
  --yl: #f59e0b;    /* 黃—警告 */
  --rd: #ef4444;    /* 紅—答錯 */
  --purple: #7c3aed; /* 紫—遊戲/XP */
}
```

### 主要元件

| 元件 | 用途 |
|------|------|
| `.tcard` | 白色內容卡片 |
| `.flashcard` | 漸層藍單字翻牌卡 |
| `.stat-box` | 首頁統計數字格 |
| `.pb-wrap + .pb` | 進度條 |
| `.fb-ok / .fb-bad` | 答題回饋（綠/紅） |
| `.streak-banner` | 打卡連續天數橫幅 |
| `.badge-item` | 勳章格（unlocked/locked） |
| `.game-pick-card` | 遊戲選擇卡片 |
| `.timer-box` | 計時器（warn/danger 狀態） |
| `.tag-n/v/adj/adv` | 詞性標籤 |
| `.speak-btn` | 單字庫播放按鈕（藍框、hover 淡藍底） |

---

## 學習路線建議（達到 850 分）

```
第 1-2 週：建立單字基礎
  ├── 每天單字卡（780-900 區間優先）
  ├── 完成文法測驗 1 次
  └── 每日打卡累積習慣

第 3-4 週：強化聽力 + 閱讀
  ├── Part 2 → Part 3 → Part 4 依序練習
  ├── 閱讀 4 篇文章 + 錯題複習
  └── 正確率目標達 80%

第 5-6 週：模擬考衝刺
  ├── 每天一次計時模擬考（50 題 / 30 分）
  ├── 集中複習錯題
  └── 正確率達 87% → 預估 850+
```

---

## 部署資訊

| 項目 | 內容 |
|------|------|
| 平台 | Streamlit Community Cloud |
| GitHub | https://github.com/carolkao-fin/toeic-pro |
| 分支 | master |
| 主程式 | app.py |
| 依賴 | streamlit>=1.31.0 |

**新增功能流程：**
1. 修改 `app.py`（本機）
2. `git add` → `git commit` → `git push`
3. Streamlit Cloud 自動重新部署（約 1 分鐘）

---

## 資料來源與授權

| 資源 | 來源 | 授權 |
|------|------|------|
| 詞彙資料集 | [kknono668/toeic-vocab-tw](https://huggingface.co/datasets/kknono668/toeic-vocab-tw)（Hugging Face） | CC-BY-SA-4.0 |
| 文法題目 | 自建（依照 TOEIC Part 5 題型設計） | — |
| 閱讀文章 | 自建（依照 TOEIC Part 7 題型設計） | — |
| 聽力題目 | 自建（依照 TOEIC Part 2/3/4 題型設計） | — |

---

## 未來可擴充方向

- [ ] 串接 Claude API：真正 AI 解析 + 個人化學習建議
- [ ] Part 1 圖片描述題（搭配圖片庫）
- [ ] 更多閱讀文章（Double / Triple Passage）
- [ ] 後端資料庫（多裝置同步學習紀錄）
- [ ] 每日推播學習提醒
- [ ] 分享成績卡片功能
- [ ] 單字間隔重複系統（Spaced Repetition / SRS）
- [x] 筆記本（自由輸入筆記）
- [x] 自訂單字庫（整合至單字卡與單字庫）
