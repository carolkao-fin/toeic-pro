# 多益學習平台 TOEIC Pro — 開發文件

## 專案概述

| 項目 | 內容 |
|------|------|
| 專案名稱 | TOEIC Pro 750+ 多益學習平台 |
| 開發工具 | Claude Code (claude-sonnet-4-6) |
| 開發日期 | 2026-05-07 |
| 技術架構 | 純前端單頁應用（HTML + CSS + JavaScript） |
| 目標 | 幫助使用者達到多益 750 分以上 |

---

## 開發歷程

### 第一版（基礎版）

**使用者需求：**
> 製作一個可以學習多益的網站

**實作功能：**
- 單字卡翻牌練習（40個單字）
- Part 5 文法測驗（12題）
- Part 7 閱讀測驗（2篇文章）
- 單字庫查詢
- 基本進度統計（localStorage）

**技術決策：**
- 選擇純 HTML 單檔案，不依賴任何外部 CDN，確保離線可用
- 使用 CSS Custom Properties（變數）統一管理設計系統
- 利用 CSS `perspective` + `rotateY` 實現卡片翻面動畫
- localStorage 持久化學習紀錄

---

### 第二版（進階版）

**使用者需求：**
> 增加更多題目、聽力練習模擬、AI 自動出題、計時模擬測驗、錯誤單字複習模式，要超越 750 分以上

**新增功能清單：**

| 功能 | 技術實作 |
|------|----------|
| 120+ 單字庫（6大分類） | 結構化 JSON 資料，含 word/pos/cat/zh/ex 欄位 |
| 30 題文法測驗 | 陣列驅動，支援隨機順序 |
| 聽力 Part 2/3/4 | Web Speech API（SpeechSynthesis） |
| 4篇閱讀文章 | 可捲動文章區塊 + 題目分離顯示 |
| 計時模擬考（50題/30分） | setInterval 倒數計時器，顏色警示 |
| 錯題複習模式 | 收集錯誤答案並進行針對性複習 |
| AI 自動出題 | 模板引擎 + 單字庫隨機生成填空題 |
| 各科分析圖表 | 純 CSS 進度條視覺化 |
| 預估多益分數 | 正確率對應分數區間公式 |

---

## 系統架構

```
index.html
├── <style>          CSS 樣式（設計系統 + 各頁面樣式）
├── <body>
│   ├── <nav>        頂部導覽列（8個頁面切換）
│   ├── #page-home   首頁儀表板
│   ├── #page-vocab  單字卡練習
│   ├── #page-grammar 文法測驗
│   ├── #page-listening 聽力練習
│   ├── #page-reading 閱讀測驗
│   ├── #page-mock   計時模擬考
│   ├── #page-review 錯題複習
│   ├── #page-wordlist 單字庫
│   └── #ai-modal    AI 出題彈窗
└── <script>
    ├── 資料層 (VOCAB, GRAMMAR_QS, LISTEN_P2/P3/P4, READINGS)
    ├── 狀態管理 (S object + localStorage)
    ├── 導覽控制 (goto, navBtn)
    ├── 單字卡模組 (initVocab, showCard, flipCard, markCard)
    ├── 文法模組 (initGrammar, renderGrammar, answerGrammar)
    ├── 聽力模組 (speak, renderP2/P3/P4, answerP2/P3/P4)
    ├── 閱讀模組 (initReading, renderRead, answerRead)
    ├── 模擬考模組 (startMock, buildMockQs, renderMock, finishMock)
    ├── 錯題複習模組 (initReview, renderReview)
    ├── 單字庫模組 (initWordlist, filterWords)
    └── AI 出題模組 (aiGenerate, renderAI, answerAI)
```

---

## 資料設計

### 單字資料結構

```javascript
{
  word: "allocate",       // 英文單字
  pos: "v.",              // 詞性
  cat: "財務會計",        // 分類（6大類）
  zh: "分配、撥出",       // 中文意思
  ex: "The budget was allocated for new equipment."  // 例句
}
```

**6大分類 × 單字數量：**

| 分類 | 單字數 | 重點考點 |
|------|--------|----------|
| 商務辦公 | 25個 | 動詞原形、被動語態 |
| 財務會計 | 20個 | 名詞、數字表達 |
| 行銷業務 | 15個 | 複合名詞、搭配詞 |
| 人力資源 | 15個 | 動詞時態、情態動詞 |
| 旅遊交通 | 13個 | 介系詞搭配 |
| 法律合規 | 12個 | 被動語態、固定片語 |

### 文法題型分布（30題）

| 考點 | 題數 |
|------|------|
| 動詞時態（過去式、完成式、被動） | 8題 |
| 介系詞搭配（at/in/on/for/with） | 5題 |
| 詞性辨別（名詞/動詞/形容詞/副詞） | 6題 |
| 連接詞與從屬子句 | 4題 |
| 關係代名詞 | 2題 |
| 固定片語（take effect / comply with） | 5題 |

### 聽力題型結構

```
Part 2 應答題（10題）
  └── 聽一個問句 → 從三個選項選出最佳應答

Part 3 對話題（2段對話 × 3題）
  └── 聽男女對話 → 回答理解問題

Part 4 獨白題（2篇獨白 × 3題）
  └── 聽公司廣播/機場廣播 → 回答理解問題
```

### 閱讀文章類型

| 文章 | 題型 | 難度 |
|------|------|------|
| Email: Project Update | 主旨、細節、詞彙 | ★★☆ |
| Notice: Office Renovation | 細節、推論 | ★★☆ |
| Advertisement: Job Opening | 職缺資訊、NOT題 | ★★★ |
| Memo: Remote Work Policy | 政策細節、時間 | ★★★ |

---

## 核心功能實作說明

### 1. 聽力模擬（Web Speech API）

```javascript
function speak(text, onEnd) {
  const u = new SpeechSynthesisUtterance(text);
  u.lang = 'en-US';
  u.rate = 0.88;   // 比正常語速稍慢，貼近真實考試
  u.pitch = 1;
  const voices = window.speechSynthesis.getVoices();
  const en = voices.find(v => v.lang.startsWith('en'));
  if (en) u.voice = en;
  if (onEnd) u.onend = onEnd;
  window.speechSynthesis.speak(u);
}
```

**Part 3 對話多行播放邏輯：**
```javascript
// 遞迴播放每一行對話，播完才顯示題目
function next() {
  if (i >= lines.length) { showScript(); return; }
  speak(lines[i++], next);
}
next();
```

### 2. 計時模擬考

```javascript
// 50題來源分配
function buildMockQs() {
  const qs = [];
  // 文法 20題（從30題庫隨機抽）
  GRAMMAR_QS.sort(() => Math.random() - .5).slice(0, 20).forEach(q => qs.push({...q, type: 'grammar'}));
  // 閱讀 20題（從4篇文章題目隨機抽）
  rdQs.sort(() => Math.random() - .5).slice(0, 20).forEach(q => qs.push(q));
  // 單字 10題（AI動態生成，從120個單字隨機選）
  vShuf.slice(0, 10).forEach(w => qs.push(generateMCQ(w)));
  return qs.sort(() => Math.random() - .5);  // 打亂順序
}
```

**計時器顏色警示：**
- 正常：藍色（> 10分鐘）
- 警告：黃色（5-10分鐘）
- 緊急：紅色（< 5分鐘）

### 3. 錯題追蹤系統

```javascript
// 答錯時記錄
function addMistake(type, q, correct, chosen) {
  S.mistakes.push({ type, q, correct, chosen, time: Date.now() });
  if (S.mistakes.length > 100) S.mistakes.shift();  // 最多保留100筆
  saveS();
}

// 持久化到 localStorage
function saveS() {
  localStorage.setItem('toeic_s2', JSON.stringify(S));
}
```

### 4. AI 自動出題

**實作原理：** 模板引擎 × 單字庫隨機配對

```javascript
const TEMPLATES = [
  (w) => ({ q: `The company decided to _______ the new regulation.`, fill: w.word }),
  (w) => ({ q: `She was praised for being highly _______ in her work.`, fill: w.word }),
  (w) => ({ q: `We need to _______ our resources more effectively.`, fill: w.word }),
];

function genOpts(w) {
  // 找相同詞性的其他單字當干擾選項
  const wrongs = VOCAB.filter(x => x.pos === w.pos && x !== w)
                      .sort(() => Math.random() - .5).slice(0, 3);
  const opts = [w.word, ...wrongs.map(x => x.word)].sort(() => Math.random() - .5);
  return { opts, ans: opts.indexOf(w.word) };
}
```

### 5. 預估多益分數公式

```javascript
function estTOEIC() {
  const acc = S.correct / S.total;
  if (acc >= 0.95) return '950+';
  if (acc >= 0.87) return '~880';
  if (acc >= 0.78) return '~790';  // 750+ 目標區間
  if (acc >= 0.68) return '~700';
  if (acc >= 0.58) return '~620';
  return '~520';
}
```

**達到 750 分的正確率目標：** 整體正確率需穩定在 **78% 以上**

---

## 設計系統

### 色彩變數

```css
:root {
  --pr: #2563eb;    /* 主色（藍）— 重點元素 */
  --gr: #10b981;    /* 綠色 — 答對、成功 */
  --yl: #f59e0b;    /* 黃色 — 警告、複習 */
  --rd: #ef4444;    /* 紅色 — 答錯、緊急 */
  --bg: #f1f5f9;    /* 頁面背景 */
  --cd: #ffffff;    /* 卡片背景 */
  --tx: #1e293b;    /* 主要文字 */
  --mu: #64748b;    /* 輔助文字 */
  --bd: #e2e8f0;    /* 邊框 */
  --ra: 12px;       /* 圓角半徑 */
}
```

### 元件清單

| 元件 | 用途 |
|------|------|
| `.card` | 白色內容區塊 |
| `.btn` | 按鈕（btn-pr/gr/rd/yl/out/sm） |
| `.option` | 選項行（支援 correct/wrong/disabled 狀態） |
| `.pb-wrap + .pb` | 進度條 |
| `.flashcard` | 3D 翻牌卡片 |
| `.listen-box` | 聽力播放區塊 |
| `.timer-box` | 計時器（支援 warning/danger 狀態） |
| `.feedback-box` | 答題回饋（ok/bad 兩種樣式） |
| `.menu-card` | 首頁功能入口卡片 |
| `.tag` | 詞性標籤（n/v/adj/adv） |

---

## 學習路線建議（達到 750 分）

```
第 1-2 週：建立基礎
  ├── 每天完成 1 輪單字卡（全部120個）
  ├── 完成文法測驗 1 次
  └── 錯題複習 1 次

第 3-4 週：強化弱點
  ├── 針對正確率最低的分項加強
  ├── 每天聽力練習（Part 2 → Part 3 → Part 4）
  └── 閱讀所有4篇文章

第 5-6 週：模擬考衝刺
  ├── 每天一次計時模擬考（50題/30分）
  ├── 複習所有錯題
  └── 正確率達 78% → 預估 750+
```

---

## 瀏覽器相容性

| 功能 | Chrome | Firefox | Safari | Edge |
|------|--------|---------|--------|------|
| 基本練習 | ✅ | ✅ | ✅ | ✅ |
| 聽力 TTS | ✅ | ✅ | ✅ | ✅ |
| localStorage | ✅ | ✅ | ✅ | ✅ |
| CSS 動畫 | ✅ | ✅ | ✅ | ✅ |

> **注意：** 聽力功能需瀏覽器支援 `window.speechSynthesis`（現代瀏覽器皆支援）。首次使用時語音引擎需載入，建議重新整理後再播放。

---

## 檔案結構

```
toeic/
└── index.html    # 完整單頁應用（所有功能整合於此檔）
```

---

## 未來可擴充方向

- [ ] 串接 Claude API 實現真正 AI 出題與個人化解析
- [ ] Part 1 圖片描述題（搭配圖片庫）
- [ ] 語音輸入練習（口說訓練）
- [ ] 後端資料庫（多裝置同步學習紀錄）
- [ ] 每日學習提醒（Service Worker / PWA）
- [ ] 分享成績卡片功能
- [ ] 更多閱讀文章（Double/Triple Passage）
